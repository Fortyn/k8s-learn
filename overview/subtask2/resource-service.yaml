apiVersion: v1
kind: Service
metadata:
  name: resource-service
  namespace: k8s-program
spec:
  selector:
    app: resources-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
  